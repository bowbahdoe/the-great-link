name: Generate "The Great Link" for Ubuntu
'on':
  workflow_dispatch:
  schedule:
    - cron: 7 2 * * *
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          java-version: '23'
          distribution: 'adopt'
      - uses: extractions/setup-just@v2
      - name: Generate Linked Image
        run: just generate